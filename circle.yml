machine:
  ruby:
    version: 2.1.5
  environment:
    CACHE: yes
test:
  override:
    - bundle exec rake circle:
        parallel: true
        files:
          - features/export.feature
          - features/import.feature
