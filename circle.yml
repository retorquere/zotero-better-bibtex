machine:
  ruby:
    version: 2.1.3
test:
  override:
    - mkdir -p $CIRCLE_TEST_REPORTS/cucumber
    - CACHE=yes bundle exec cucumber --format json --out $CIRCLE_TEST_REPORTS/cucumber/tests.cucumber
