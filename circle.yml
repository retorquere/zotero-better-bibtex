machine:
  ruby:
    version: "2.2"
dependencies:
  cache_directories:
    - "bin"
    - test/fixtures/profiles
test:
  override:
    - ./circle.sh build:
        parallel: true
deployment:
  master:
    branch: master
    commands:
      - ./circle.sh deploy
