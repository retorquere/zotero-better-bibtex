machine:
  ruby:
    version: 2.1.5
  environment:
    CACHE: yes
test:
  override:
    - case $CIRCLE_NODE_INDEX in 0) bundle exec rake test[export] ;; 1) bundle exec rake test[import] ;; 2) bundle exec rake test[bulk] ;; esac:
        parallel: true

